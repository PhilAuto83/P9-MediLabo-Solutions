spring:
  application:
    name: mls-gateway
  cloud:
      gateway:
        mvc:
          routes:
          - id: coordonnees-patient
            uri: lb://mls-donnees-patient
            predicates:
              - Path=gateway/**
            filters:
              - StripPrefix=1
  security:
    user:
      name: phildev
      password: mls2024@
server:
  port: 8081
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8089/eureka


